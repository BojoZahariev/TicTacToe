const gameBoard = (() => {
    var boardArray = ['x', 'x', 'x', 'x', 'x', 'x', 'x', 'x', 'x'];

    const div = (a, b) => a / b;
    return {
        boardArray
    };
})();
console.log(gameBoard.boardArray);
